export type SessionData = {
  _id: string;
  title: string;
  sessionDate: string;
  clientId: string;
  caseId: string;
  sessionLocation: string;
  sessionTime: string;
  courtName:string;
  createdAt: string;
  updatedAt: string;
  note: string;
};
